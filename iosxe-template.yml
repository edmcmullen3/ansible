---    
- name: Write hostname
  hosts: routers
  {{ lookup('file','vars_global.yaml') | from_yaml }}
  tasks:
  - name: write hostname using jinja2
    ansible.builtin.template:
       src: template_global.j2
       dest: ./config/{{ inventory_hostname }}.txt
