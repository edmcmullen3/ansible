---
- name: Apply the provided configuration
  hosts: routers
  tasks:
  - name: ntp
    state: replaced
    cisco.ios.ios_ntp_global:
      config:
        servers:
          - server: "{{ item }}"
    loop: "{{ ntp_servers }}"